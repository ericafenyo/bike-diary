query GetAdventureById($adventureId: String!) {
  adventure(id: $adventureId) {
     id
     title
     description
     startTime
     endTime
     distance
     duration
     speed
     calories
     polyline
  }
}
