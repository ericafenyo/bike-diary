mutation CreateAdventures($params: [AdventureInput!]!) {
  createAdventures(params: $params) {
    id
    description
    title
    calories
    distance
    duration
    startTime
    endTime
    speed
    polyline
  }
}